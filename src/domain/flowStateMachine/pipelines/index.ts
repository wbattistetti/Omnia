/**
 * Domain pipeline modules for the flow state machine (structural graph, variable projection, subflow wiring).
 * Business logic remains in `domain/structural` and `domain/taskSubflowMove`; this folder is the composition seam.
 */

export {};
