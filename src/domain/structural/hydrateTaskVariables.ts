/**
 * Targeted hydration helpers — structural pipeline uses {@link reconcileUtteranceVariableStoreWithFlowGraph}
 * in the orchestrator; this module documents extension points for future per-task reconciliation.
 */

export { reconcileUtteranceVariableStoreWithFlowGraph } from './reconcileVariableStore';
