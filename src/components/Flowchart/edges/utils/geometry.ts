/**
 * Geometry Module - EMPTY
 *
 * All geometric calculations have been moved to discrete hit-area model.
 * No continuous calculations, no projections, no sampling.
 *
 * This file is kept for backward compatibility but contains no functions.
 *
 * ✅ NEW MODEL: Discrete functions only:
 * - getSegmentMidpoint() from pathUtils.ts
 * - Direct absolute coordinates {x, y}
 */

// All functions removed - discrete model uses only:
// - getSegmentMidpoint() from pathUtils.ts
// - Direct absolute coordinates {x, y}
