// Please write clean, production-grade TypeScript code.
// Avoid non-ASCII characters, Chinese symbols, or multilingual output.

/**
 * Node constants used throughout the Grammar Editor.
 * Centralized for easy maintenance and consistency.
 */
export const NODE_FONT = '13px sans-serif';
export const NODE_PADDING_H = 12; // 6px left + 6px right
export const NODE_MIN_WIDTH = 32;
export const NODE_PLACEHOLDER = 'Type word...';

/**
 * Estimated node dimensions for initial positioning.
 * Used when creating nodes on canvas double-click.
 */
export const ESTIMATED_NODE_WIDTH = 40;
export const ESTIMATED_NODE_HEIGHT = 20;
