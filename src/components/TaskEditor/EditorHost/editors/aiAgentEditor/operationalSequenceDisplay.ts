/**
 * Operational sequence display — re-exports from structuredSectionDisplay (backward compatible).
 */

export {
  formatOperationalSequenceNewlines,
  splitOperationalSequenceLines,
} from './structuredSectionDisplay';
