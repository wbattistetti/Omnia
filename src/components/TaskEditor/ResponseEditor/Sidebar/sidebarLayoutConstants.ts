/**
 * Sidebar width: must match the default grid track in ResponseEditorNormalLayout
 * so the inner measured width does not jump (e.g. 288 → 200) when the first empty row appears.
 */
export const SIDEBAR_CONTENT_MIN_WIDTH_PX = 288;
