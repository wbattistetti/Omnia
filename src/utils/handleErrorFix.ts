/**
 * @deprecated Import from `compilationErrorFix` (`runCompilationErrorFix`) — this file re-exports for backward compatibility.
 */
export { runCompilationErrorFix as handleErrorFix } from './compilationErrorFix';
