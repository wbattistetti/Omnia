Option Strict On
Option Explicit On

''' <summary>
''' Placeholder: nessun scenario manuale cablato. Il flusso reale passa da ApiServer, compilatore e test.
''' </summary>
Module Program

    Sub Main(args As String())
        Console.WriteLine("UtteranceInterpretation.Demo — esempio interattivo rimosso.")
        Console.WriteLine("Usare ApiServer, il compilatore (task.Engines) e i test di progetto per l'integrazione.")
    End Sub

End Module
