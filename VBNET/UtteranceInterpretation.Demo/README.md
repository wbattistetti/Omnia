# UtteranceInterpretation.Demo

Progetto console minimale (nessuno scenario manuale cablato). L’integrazione del nuovo interprete avviene tramite **ApiServer**, **compilatore** (`CompiledUtteranceTask.Engines`) e **test**.

```bash
dotnet run -c Release
```

Stampa solo un messaggio informativo.
