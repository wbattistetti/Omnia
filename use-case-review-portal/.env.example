# Opzionale — in dev il token viene letto da backend/.env in automatico.
# Serve solo per override o build di produzione (architetto di sistema).
# VITE_AGENT_REVIEW_CHANNEL_TOKEN=
# Vuoto = proxy Vite verso Express in dev
VITE_REVIEW_API_BASE=
VITE_REVIEW_API_TARGET=http://127.0.0.1:3100
