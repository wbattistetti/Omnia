/**
 * Chunked use-case bundle generation limits (initial + extend batches).
 */

const USE_CASE_BUNDLE_CHUNK_SIZE = 5;
const USE_CASE_BUNDLE_MAX_TOTAL = 15;

module.exports = {
  USE_CASE_BUNDLE_CHUNK_SIZE,
  USE_CASE_BUNDLE_MAX_TOTAL,
};
