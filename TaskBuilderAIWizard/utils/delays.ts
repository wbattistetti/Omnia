// SimulationSpeed type - kept for compatibility with useWizardState
// Note: delay functions removed as they were only used for fake simulations
export type SimulationSpeed = 'fast' | 'medium' | 'slow';
